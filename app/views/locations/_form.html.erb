<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Address" %><br />
    <%= f.text_field :address_string %>
  </div>
  <div class="field">
    <%= f.label :start %><br />
    <%= f.check_box :start %>
  </div>
  <div class="field">
    <%= f.label :finish %><br />
    <%= f.check_box :finish %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
